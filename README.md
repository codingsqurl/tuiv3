# tuiv3
2 dudes made a tui

## Setup

Only dependency: [uv](https://docs.astral.sh/uv/getting-started/installation/)

```
git clone <repo>
cd tuiv3
uv run main.py
```

That's it. `uv run` handles the Python version, venv, and packages automatically.

## Tests

```
uv run pytest
```
